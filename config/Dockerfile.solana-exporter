FROM python:3.9-slim

WORKDIR /app

# Copy the Solana exporter script
COPY solana-exporter.py .

# Install dependencies
RUN pip install requests prometheus_client

# Expose metrics port
EXPOSE 8080

# Run the exporter
CMD ["python", "solana-exporter.py"]
