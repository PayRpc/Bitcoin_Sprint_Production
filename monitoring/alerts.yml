groups:
- name: sprint-basic
  interval: 15s
  rules:
  - alert: SprintTargetDown
    expr: up{job=~"bitcoin-sprint-.*"} == 0
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "Scrape target {{ $labels.job }} is down"
